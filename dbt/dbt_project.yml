name: lp_history
version: "1.0.0"
profile: lp_history

model-paths: ["models"]
test-paths: ["tests"]

models:
  lp_history:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
